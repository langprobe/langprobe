# Copy to infra/.env before `docker compose up`.
# Generate SESSION_SECRET with: openssl rand -hex 32
SESSION_SECRET=

# Optional overrides
POSTGRES_PASSWORD=langprobe
CLICKHOUSE_PASSWORD=langprobe
LOG_LEVEL=INFO
WEB_ORIGIN=http://localhost:7090
API_BASE=http://localhost:7081
