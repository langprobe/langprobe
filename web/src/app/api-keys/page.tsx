import { ComingSoon } from "@/components/ComingSoon";

export default function ApiKeysPage() {
  return (
    <ComingSoon
      title="API keys"
      blurb="Issue and rotate per-project API keys for the SDK. Scoped to a single project, revocable in one click, and audited so you can prove who shipped which trace."
    />
  );
}
