import { ComingSoon } from "@/components/ComingSoon";

export default function EvalsPage() {
  return (
    <ComingSoon
      title="Evals"
      blurb="Run scored evaluations against your agents. LLM-as-judge, custom checkers, and pass/fail rubrics — versioned alongside the prompt that produced them."
    />
  );
}
