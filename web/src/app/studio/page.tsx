import { ComingSoon } from "@/components/ComingSoon";

export default function StudioPage() {
  return (
    <ComingSoon
      title="Studio"
      blurb="Build and tune agent flows in a visual canvas. Branch on a real trace, edit a node, replay against the same fixture — round-trip with the prompts and evals views."
    />
  );
}
