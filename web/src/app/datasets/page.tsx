import { ComingSoon } from "@/components/ComingSoon";

export default function DatasetsPage() {
  return (
    <ComingSoon
      title="Datasets"
      blurb="Curate evaluation datasets from production traces. Pin failures as regression cases, sample by tag or session, and version everything in source control."
    />
  );
}
