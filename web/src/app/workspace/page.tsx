import { ComingSoon } from "@/components/ComingSoon";

export default function WorkspacePage() {
  return (
    <ComingSoon
      title="Workspace"
      blurb="Manage projects, members, billing limits, retention windows, and PII redaction policy. Self-hosted by default — your traces never leave your infra."
    />
  );
}
