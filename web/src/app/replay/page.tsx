import { ComingSoon } from "@/components/ComingSoon";

export default function ReplayPage() {
  return (
    <ComingSoon
      title="Replay"
      blurb="Step through any captured agent run frame-by-frame. Mutate prompts, swap models, replay tool calls — and see the divergence inline. The debugger half of tracability."
    />
  );
}
