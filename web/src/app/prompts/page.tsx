import { ComingSoon } from "@/components/ComingSoon";

export default function PromptsPage() {
  return (
    <ComingSoon
      title="Prompts"
      blurb="Version, diff, and roll back prompts without redeploying. Tag a winning revision, A/B against production, and tie every run back to the exact prompt that produced it."
    />
  );
}
